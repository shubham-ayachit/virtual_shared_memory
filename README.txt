Edit server as per your address in func.h

Use of shared_array as abstraction of virtual memory shared over different clients.

mutex locks are used over multithreading to access static array and lock the indexes accordingly.
